<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive launcher background: flat black, matching the tile the ouroboros mark was drawn on.
     Flat rather than the old gradient because the mark is fine white line work — any tone behind it
     competes with the lines at launcher size, and the artwork it came from was black for that reason. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp" android:height="108dp"
    android:viewportWidth="108" android:viewportHeight="108">
    <path android:pathData="M0,0h108v108h-108z" android:fillColor="#000000"/>
</vector>
